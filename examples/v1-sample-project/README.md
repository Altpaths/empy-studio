# Empy Studio v1 Sample Project

This project demonstrates a controlled task with repository instructions,
a Task Contract, a runtime manifest, a user input, expected outputs, and
evidence.

Run:

```bash
empy runtime run \
  --manifest runtime-manifest.json \
  --output-root outputs
```

Expected files:

```text
outputs/result.json
outputs/evidence.json
```
