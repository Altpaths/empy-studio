# Empy Studio Example Project

Work only inside the provided scenario output directory.

The task is complete only when:

1. `result.json` exists.
2. `evidence.json` exists.
3. No source file outside the scenario output directory is modified.
